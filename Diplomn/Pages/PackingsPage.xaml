<Page x:Class="Diplomn.Pages.PackingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:addons="clr-namespace:Diplomn.Addons"
      Title="Фасовка"
      Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Фасовка" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="Управление фасовкой товаров" FontSize="16" Opacity="0.7"
                           Foreground="{DynamicResource ForegroundBrush}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtSearch" Height="32" FontSize="19"
                                 addons:PlaceholderBehavior.PlaceholderText="Поиск по количеству..."
                                 KeyDown="TxtSearch_KeyDown"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                            <Button Content="🔍" Width="36" Height="32" Click="ApplyFilters_Click"/>
                            <Button Content="✕" Width="36" Height="32" Click="ClearFilters_Click" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <DataGrid Grid.Row="1" x:Name="DataGridPackings"
                          SelectionChanged="DataGridPackings_SelectionChanged"
                          AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn FontSize="18" Header="Код" Binding="{Binding Код_фасовки}" Width="60"/>
                        <DataGridTextColumn FontSize="18" Header="Количество" Binding="{Binding Количество}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Border Grid.Column="1" Width="1" Background="{DynamicResource BorderBrush}" 
                    Opacity="0.2" Margin="15,0"/>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Редактирование фасовки" FontWeight="Bold" FontSize="14" 
                           Margin="0,0,0,15" Foreground="{DynamicResource ForegroundBrush}"/>

                <TextBlock Text="Код" FontSize="19" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtPackingId" IsReadOnly="True" Height="32" Margin="0,0,0,12" FontSize="19"
                         addons:PlaceholderBehavior.PlaceholderText="Код фасовки" 
                         Focusable="False" Cursor="Arrow" Opacity="0.4" 
                         ToolTip="Заполняется автоматически"/>

                <TextBlock Text="Количество *" FontSize="19" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtPackingQuantity" Height="32" Margin="0,0,0,15" FontSize="19"
                         addons:PlaceholderBehavior.PlaceholderText="Введите количество"/>

                <WrapPanel x:Name="ActionButtonsPanel" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Блок уведомлений -->
        <Border x:Name="SuccessBorder" 
                Grid.Row="2"
                Background="{DynamicResource SuccessBrush}" 
                CornerRadius="4" 
                Padding="12,10" 
                Margin="0,15,0,0"
                Visibility="Collapsed">
            <TextBlock x:Name="SuccessText" 
                       Foreground="{DynamicResource ForegroundBrush}" 
                       HorizontalAlignment="Center" 
                       TextWrapping="Wrap"
                       FontSize="13"/>
        </Border>
    </Grid>
</Page>