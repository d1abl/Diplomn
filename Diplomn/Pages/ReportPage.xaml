<Page x:Class="Diplomn.Pages.ReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="ReportPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Отчеты" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Панель фильтров -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Отчет по продажам -->
            <Border Grid.Column="0" Background="#F5F5F5" Padding="15" Margin="5" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Отчет по продажам" FontSize="14" FontWeight="Bold" Foreground="#F44336" Margin="0,0,0,10"/>
                    <TextBlock Text="Дата с:" Margin="0,5,0,0"/>
                    <DatePicker x:Name="DateFromSales" Height="35" Margin="0,0,0,5"/>
                    <TextBlock Text="Дата по:" Margin="0,5,0,0"/>
                    <DatePicker x:Name="DateToSales" Height="35" Margin="0,0,0,10"/>
                    <Button Content="📊 Сформировать отчет" Click="GenerateSalesReport_Click" Height="35"/>
                </StackPanel>
            </Border>

            <!-- Отчет по заказам -->
            <Border Grid.Column="1" Background="#F5F5F5" Padding="15" Margin="5" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Отчет по заказам" FontSize="14" FontWeight="Bold" Foreground="#9C27B0" Margin="0,0,0,10"/>
                    <TextBlock Text="Дата с:" Margin="0,5,0,0"/>
                    <DatePicker x:Name="DateFromSupplies" Height="35" Margin="0,0,0,5"/>
                    <TextBlock Text="Дата по:" Margin="0,5,0,0"/>
                    <DatePicker x:Name="DateToSupplies" Height="35" Margin="0,0,0,10"/>
                    <Button Content="📊 Сформировать отчет" Click="GenerateSuppliesReport_Click" Height="35"/>
                </StackPanel>
            </Border>

            <!-- Отчет по товарам -->
            <Border Grid.Column="2" Background="#F5F5F5" Padding="15" Margin="5" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Отчет по товарам" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Категория:" Margin="0,5,0,0"/>
                    <ComboBox x:Name="CmbCategory" DisplayMemberPath="Категория" Height="35" Margin="0,0,0,10"/>
                    <Button Content="📊 Сформировать отчет" Click="GenerateProductsReport_Click" Height="35"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Результаты отчета -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock x:Name="TxtReportTitle" FontSize="14" FontWeight="Bold" Margin="0,10,0,10"/>
                <DataGrid x:Name="DataGridReport" AutoGenerateColumns="True"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          Margin="0,5"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Итоговая строка -->
        <Border Grid.Row="3" Background="#F0F0F0" Padding="10" Margin="0,10,0,0" CornerRadius="5">
            <TextBlock x:Name="TxtReportSummary" FontSize="13" FontWeight="Bold" Foreground="Gray" TextAlignment="Center"/>
        </Border>
    </Grid>
</Page>