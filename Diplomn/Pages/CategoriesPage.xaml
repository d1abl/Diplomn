<Page x:Class="Diplomn.Pages.CategoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="CategoriesPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Управление категориями" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая панель: таблица категорий -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Список категорий" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                <DataGrid x:Name="DataGridCategories" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          Height="300" 
                          Margin="0,5,0,0"
                          SelectionChanged="DataGridCategories_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Код" Binding="{Binding Код_категория}" Width="60"/>
                        <DataGridTextColumn Header="Категория" Binding="{Binding Категория}" Width="150"/>
                        <DataGridTextColumn Header="Описание" Binding="{Binding Описание_категории}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <Border Grid.Column="1" Width="2" Background="#E0E0E0" Margin="10,0"/>

            <!-- Правая панель: форма редактирования -->
            <StackPanel Grid.Column="2" Margin="10,0,0,0">
                <TextBlock Text="Редактирование категории" FontWeight="Bold" FontSize="14" Margin="0,0,0,20"/>

                <TextBlock Text="Код:" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtCategoryId" IsReadOnly="True" Background="#F0F0F0" Height="30"/>

                <TextBlock Text="Название категории:" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtCategoryName" Height="30"/>

                <TextBlock Text="Описание:" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtDescription" 
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Height="100"
                         VerticalScrollBarVisibility="Auto"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="➕ Добавить" Click="Add_Click" Width="100" Margin="5"/>
                    <Button Content="💾 Обновить" Click="Update_Click" Width="100" Margin="5"/>
                    <Button Content="🗑 Удалить" Click="Delete_Click" Width="100" Margin="5"/>
                    <Button Content="❌ Очистить" Click="ClearForm_Click" Width="100" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="2" Text="Двойной клик по строке таблицы для быстрого редактирования" 
                   Foreground="Gray" FontSize="12" TextAlignment="Center"/>
    </Grid>
</Page>