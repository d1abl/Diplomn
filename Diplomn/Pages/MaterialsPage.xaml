<Page x:Class="Diplomn.Pages.MaterialsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:addons="clr-namespace:Diplomn.Addons"
      Title="Материалы"
      Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Материалы" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="Управление материалами" FontSize="13" Opacity="0.7"
                           Foreground="{DynamicResource ForegroundBrush}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtSearch" Height="32"
                                 addons:PlaceholderBehavior.PlaceholderText="Поиск по названию..."
                                 KeyDown="TxtSearch_KeyDown"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                            <Button Content="🔍" Width="36" Height="32" Click="ApplyFilters_Click"/>
                            <Button Content="✕" Width="36" Height="32" Click="ClearFilters_Click" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <DataGrid Grid.Row="1" x:Name="DataGridMaterials"
                          SelectionChanged="DataGridMaterials_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Код" Binding="{Binding Код_материала}" Width="60"/>
                        <DataGridTextColumn Header="Наименование" Binding="{Binding Наименование_материала}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Border Grid.Column="1" Width="1" Background="{DynamicResource BorderBrush}" 
                    Opacity="0.2" Margin="15,0"/>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Редактирование материала" FontWeight="Bold" FontSize="14" 
                           Margin="0,0,0,15" Foreground="{DynamicResource ForegroundBrush}"/>

                <TextBlock Text="Код" FontSize="12" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtMaterialId" IsReadOnly="True" Height="32" Margin="0,0,0,12"
                         addons:PlaceholderBehavior.PlaceholderText="Код материала"/>

                <TextBlock Text="Наименование *" FontSize="12" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtMaterialName" Height="32" Margin="0,0,0,15"
                         addons:PlaceholderBehavior.PlaceholderText="Введите название"/>

                <WrapPanel HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="Добавить" Click="Add_Click" Width="90" Height="34" Margin="3"/>
                    <Button Content="Обновить" Click="Update_Click" Width="90" Height="34" Margin="3"/>
                    <Button Content="Удалить" Click="Delete_Click" Width="90" Height="34" Margin="3"/>
                    <Button Content="Очистить" Click="ClearForm_Click" Width="90" Height="34" Margin="3"/>
                </WrapPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>